version: "3.9"

services:
  basquenland-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: basquenland-web
    ports:
      - "8085:80"
    volumes:
      - ./data:/var/www/html/data
    restart: unless-stopped
